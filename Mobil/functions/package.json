{
  "name": "nge-producto4-functions",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "tsc --noEmit",
    "build": "tsc",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start"
  },
  "dependencies": {
    "expo-server-sdk": "^6.1.0",
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0"
  },
  "main": "lib/index.js",
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
